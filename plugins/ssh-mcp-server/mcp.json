{
"ssh-mcp-server": {
    "type": "stdio",
    "command": "npx",
    "args": [
      "--registry", "https://registry.npmmirror.com",
      "-y",
      "@fangjunjie/ssh-mcp-server",
      "--config-file",
      "${SSH_MCP_CONFIG}"
    ]
  }
}